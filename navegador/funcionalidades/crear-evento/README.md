# crear-evento

**Qué cubre:** Crear evento: explicación de roles, cuenta de administrador ("Guardá tu acceso"), datos del evento, "Tu evento y la IA" (local primero, nube opcional), **idiomas de cada sala** y estimador de costo.

**Qué NO hace:** Crear la cuenta sin validar en el servidor; nunca pide API keys.

**Hoy:** bienvenida, los tres pasos, "Guardá tu acceso" y el estimador de costo. Pendiente: idiomas de cada sala (llegan con las salas) e "Instalar como app" (hace falta un manifiesto).

**Estructura:** `componentes/` · `hooks/` · `api.ts` (si llama a la API) · `index.ts` (lo único que importan las rutas).

**Maqueta de referencia:** ver la tabla de `navegador/rutas/README.md`.
