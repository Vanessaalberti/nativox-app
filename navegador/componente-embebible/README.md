# componente-embebible ♻ — `<subtitulos-en-vivo>`

Web Component sin React, compilado aparte como `/subtitulos.js`. Cualquier sitio lo agrega con `<script src="https://<instancia>/subtitulos.js">` y `<subtitulos-en-vivo sala="escenario-1" idioma="es">`. Usa `cliente-sala` y Shadow DOM.
