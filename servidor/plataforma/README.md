# plataforma

- `env.d.ts` — generado con `wrangler types` (no se edita a mano).
- `registrador.ts` — logs estructurados en JSON (`error`, `warn`, `info`), sin secretos.
- `errores.ts` — respuestas de error con el contrato común.
