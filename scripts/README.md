# Scripts

| Previsto | Qué hace |
| --- | --- |
| `semilla.sql` | Datos de ejemplo para desarrollo local (evento, salas, agenda) |
| `informe-calidad.ts` | Pasa `muestras/` por los motores y genera la tabla de WER, términos y retraso (la usa la comparación de la landing) |

Sin scripts de deploy manual: el deploy lo hace el botón.
