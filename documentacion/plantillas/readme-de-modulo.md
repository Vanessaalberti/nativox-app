# nombre-del-modulo ♻

**Qué hace:** una oración.

**Qué NO hace:** los límites (lo que alguien podría esperar y va en otro lado).

## API pública (solo desde `index.ts`)

- `funcion(entrada): salida` — qué devuelve y cuándo falla.

## Dependencias

- **Puede importar:** solo `compartido/contratos` (o nada).
- **Lo usan:** qué funcionalidades o módulos.

## Archivos previstos

- `index.ts` — API pública (lo único que se importa desde afuera)

## Pruebas

Qué casos reales cubre.

## Referencia

Sección del documento de decisiones con la técnica y las mediciones.
